<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.joongbu.femos.msf"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >

        <!-- 홈 -->
        <activity
            android:name="kr.ac.joongbu.femos.msf.HomeActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 축제 참여 -->
        <activity
            android:name="kr.ac.joongbu.femos.msf.joinfestival.JoinFestivalActivity"
            android:theme="@android:style/Theme.NoTitleBar" />

        <!-- 만족도 평가 -->
        <activity android:name="kr.ac.joongbu.femos.msf.satisfactionrating.SatisfactionRatingQRCodeReaderActivity" />
        <activity
            android:name="kr.ac.joongbu.femos.msf.satisfactionrating.SatisfactionRatingActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name="com.google.zxing.client.android.CaptureActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:windowSoftInputMode="stateAlwaysHidden" >

            <!--
          	<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            -->
            <intent-filter>
                <action android:name="com.google.zxing.client.android.SCAN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 불편 신고 -->
        <activity
            android:name="kr.ac.joongbu.femos.msf.inconveniencereport.CameraActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name="kr.ac.joongbu.femos.msf.inconveniencereport.InconvenienceReportActivity"
            android:theme="@android:style/Theme.NoTitleBar" />

        <!-- 방문객 체크 -->
        <activity
            android:name="kr.ac.joongbu.femos.msf.guestcheck.GuestCheckActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>
    
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

</manifest>